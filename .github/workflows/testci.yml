name: TestCI

on: push

jobs:
  publish:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
      with:
        repository: 'gitactions'

    - uses: mr-smithers-excellent/docker-build-push@v5
      with:
        image: ibrikdev/demo
        registry: docker.io
        username: ${{ secrets.DOCKER_IBRIKDEV_USERNAME }}
        password: ${{ secrets.DOCKER_IBRIKDEV_PASSWORD }}